<UserControl x:Class="FotocopiadoraWPF.FotocopiasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FotocopiadoraWPF"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:inputLayout="clr-namespace:Syncfusion.UI.Xaml.TextInputLayout;assembly=Syncfusion.SfTextInputLayout.WPF"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="inputLayout:SfTextInputLayout">
            <Setter Property="Margin" Value="10,0,10,10"/>
        </Style>
    </UserControl.Resources>


    <StackPanel Orientation="Horizontal">
        <Grid RenderTransformOrigin="0.372,0.297" VerticalAlignment="Top" Width="400">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Datos" FontSize="18" FontWeight="Bold" Grid.ColumnSpan="2" HorizontalAlignment="Center" />

            <inputLayout:SfTextInputLayout Grid.Row="1" Grid.Column="0"  Hint="Nombre" HelperText="Ingresa tu nombre" ContainerType="Outlined" >
                <TextBox Height="15" VerticalContentAlignment="Center" VerticalAlignment="Center" FontSize="14" />
            </inputLayout:SfTextInputLayout>

            <inputLayout:SfTextInputLayout Grid.Row="1" Grid.Column="1" Hint="Fecha" HelperText="Ingresa la fecha" ContainerType="Outlined">
                <syncfusion:DateTimeEdit IsButtonPopUpEnabled="False" Height="13" FontSize="14" MinHeight="15"/>
            </inputLayout:SfTextInputLayout>

            <inputLayout:SfTextInputLayout Grid.Row="3" Grid.Column="0"  Hint="Paginas" HelperText="Ingresa las paginas" ContainerType="Outlined" VerticalAlignment="Center">
                <syncfusion:IntegerTextBox Height="15" ValueValidation="OnKeyPress" UseNullOption="True" FontSize="14" />
            </inputLayout:SfTextInputLayout>

            <inputLayout:SfTextInputLayout Grid.Row="3" Grid.Column="1"  Hint="Anillados" HelperText="Ingresa los anillados" ContainerType="Outlined" VerticalAlignment="Center">
                <syncfusion:IntegerTextBox Height="15" ValueValidation="OnKeyPress" UseNullOption="True" FontSize="14" />
            </inputLayout:SfTextInputLayout>

            <CheckBox Grid.Row="2" Grid.Column="0" Content="Empleado" Margin="10,5,0,5"/>

            <inputLayout:SfTextInputLayout Grid.Row="4" Grid.Column="0"  Grid.ColumnSpan="2" Hint="Comentario" HelperText="" ContainerType="Outlined" VerticalAlignment="Center">
                <TextBox Height="15" FontSize="14" />
            </inputLayout:SfTextInputLayout>

            <Label Grid.Row="5" Content="Metodo de pago" FontSize="18" FontWeight="Bold" Grid.ColumnSpan="2" HorizontalAlignment="Center" />

            <inputLayout:SfTextInputLayout Grid.Row="6" Grid.Column="0"  Hint="Efectivo" HelperText="Ingresa los anillados" ContainerType="Outlined" VerticalAlignment="Center">
                <syncfusion:IntegerTextBox Height="15" ValueValidation="OnKeyPress" UseNullOption="True" FontSize="14" />
            </inputLayout:SfTextInputLayout>

            <inputLayout:SfTextInputLayout Grid.Row="6" Grid.Column="1"  Hint="Transferencia" HelperText="Ingresa los anillados" ContainerType="Outlined" VerticalAlignment="Center">
                <syncfusion:IntegerTextBox Height="15" ValueValidation="OnKeyPress" UseNullOption="True" FontSize="14" />
            </inputLayout:SfTextInputLayout>
        </Grid>
        <Grid Width="200">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Detalle" FontSize="18" FontWeight="Bold" Grid.ColumnSpan="2" HorizontalAlignment="Center" />

            <syncfusion:SfTextInputLayout Grid.Row="1" Grid.Column="0"  Hint="Paginas" HelperText="Ingresa las paginas" ContainerType="Outlined" VerticalAlignment="Center">
                <syncfusion:IntegerTextBox Height="15" FontSize="14" Text="0" IsReadOnly="True" />
            </syncfusion:SfTextInputLayout>

            <syncfusion:SfTextInputLayout Grid.Row="2" Grid.Column="0"  Hint="Anillados" HelperText="Ingresa las paginas" ContainerType="Outlined" VerticalAlignment="Center">
                <syncfusion:IntegerTextBox Height="15" FontSize="14" Text="0" IsReadOnly="True" />
            </syncfusion:SfTextInputLayout>
            <syncfusion:SfTextInputLayout Grid.Row="3" Grid.Column="0"  Hint="Total" HelperText="Ingresa las paginas" ContainerType="Outlined" VerticalAlignment="Center">
                <syncfusion:IntegerTextBox Height="15" FontSize="14" Text="0" IsReadOnly="True" />
            </syncfusion:SfTextInputLayout>
        </Grid>
    </StackPanel>
</UserControl>
