<UserControl x:Class="Views.MovimientosCajaListadoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FotocopiadoraWPF"
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:FotocopiadoraWPF.ViewModels">

    <ListView
        ItemsSource="{Binding MovimientosView}"
        BorderThickness="0"
        Background="Transparent"
        HorizontalContentAlignment="Stretch"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        ScrollViewer.VerticalScrollBarVisibility="Auto"
        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
        Width="400">
        <!-- Estilo del item -->
           <ListView.ItemContainerStyle>
               <Style TargetType="ListViewItem">
                   <Setter Property="Padding" Value="0"/>
                   <Setter Property="Margin" Value="0,6"/>
                   <Setter Property="BorderThickness" Value="0"/>
                   <Setter Property="Background" Value="Transparent"/>
                   <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
               </Style>
           </ListView.ItemContainerStyle>

           <!-- HEADER POR FECHA -->
           <ListView.GroupStyle>
               <GroupStyle>
                   <GroupStyle.HeaderTemplate>
                       <DataTemplate>
                           <TextBlock Text="{Binding Name, StringFormat=dd/MM}"
                                      FontSize="16"
                                      FontWeight="Bold"
                                      Margin="0,16,0,6"
                                      Foreground="#374151"/>
                       </DataTemplate>
                   </GroupStyle.HeaderTemplate>
               </GroupStyle>
           </ListView.GroupStyle>

           <!-- ===================== ITEM ===================== -->
           <ListView.ItemTemplate>
               <DataTemplate>

                <Border CornerRadius="12"
                       BorderBrush="#E6E8FF"
                       BorderThickness="1"
                       Padding="12"
                       Background="White">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- MOTIVO + EMPLEADO -->
                        <StackPanel>
                            <TextBlock Text="{Binding Motivo}" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock FontSize="11" Foreground="#6B7280">
                               <Run Text="{Binding Empleado}"/>
                               <Run Text=" • "/>
                               <Run Text="{Binding Fecha, StringFormat=HH:mm}"/>
                            </TextBlock>
                        </StackPanel>
                        <!-- CHIP TIPO -->
                        <Border Grid.Column="1"
                                Padding="8,3"
                                CornerRadius="12"
                                Margin="8,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#DCFCE7"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Tipo}" Value="Egreso">
                                            <Setter Property="Background" Value="#FEE2E2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <StackPanel Orientation="Horizontal"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                <!-- PUNTO -->
                                <Ellipse Width="6"
                                         Height="6"
                                         Margin="0,0,5,0"
                                         VerticalAlignment="Center">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="#059669"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Tipo}" Value="Egreso">
                                                    <Setter Property="Fill" Value="#DC2626"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <!-- TEXTO -->
                                <TextBlock Text="{Binding Tipo}"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <!-- MONTO -->
                        <TextBlock Grid.Column="2"
                                   Margin="12,0,0,0"
                                   FontWeight="Bold"
                                   FontSize="14"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center"
                                   Text="{Binding Monto, StringFormat=$ {0:N0}}"/>
                        <StackPanel Grid.Column="3"
            Orientation="Horizontal"
            Margin="10,0,0,0">

                            <Button Content="✏"
            Width="28"
            Height="28"
            Margin="0,0,5,0"
            Command="{Binding DataContext.EditarCommand,RelativeSource={RelativeSource AncestorType=UserControl}}"
            CommandParameter="{Binding}" />

                            <Button Content="🗑"
            Width="28"
            Height="28"
            Command="{Binding DataContext.EliminarCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
            CommandParameter="{Binding}" />
                            <Button Content="💳"
        Width="28"
        Height="28"
        Margin="5,0,0,0"
        Command="{Binding DataContext.PagarCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
        CommandParameter="{Binding}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Tipo}" Value="Egreso">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                        </StackPanel>

                    </Grid>
                </Border>
            </DataTemplate>
           </ListView.ItemTemplate>

       </ListView>

</UserControl>
