<UserControl x:Class="FotocopiasListadoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FotocopiadoraWPF"
             xmlns:vm="clr-namespace:FotocopiadoraWPF.ViewModels">
    
    <UserControl.DataContext>
        <vm:FotocopiasListadoViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">

        <DataGrid ItemsSource="{Binding Fotocopias}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Fecha"
                                    Binding="{Binding Fecha, StringFormat=d}" />

                <DataGridTextColumn Header="Nombre"
                                    Binding="{Binding Nombre}" />

                <DataGridTextColumn Header="Páginas"
                                    Binding="{Binding Paginas}" />

                <DataGridTextColumn Header="Anillados"
                                    Binding="{Binding Anillados}" />

                <DataGridTextColumn Header="Total"
                                    Binding="{Binding PrecioTotal, StringFormat=C0}" />

                <DataGridTextColumn Header="Efectivo"
                                    Binding="{Binding Efectivo, StringFormat=C0}" />

                <DataGridTextColumn Header="Transferencia"
                                    Binding="{Binding Transferencia, StringFormat=C0}" />

                <DataGridTextColumn Header="Estado"
                                    Binding="{Binding Estado}" />

                <DataGridTextColumn Header="Comentario"
                                    Binding="{Binding Comentario}" />

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>
